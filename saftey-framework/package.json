{
  "name": "saftey-framework",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "i18n": "node src/build-i18n.js",
    "dev": "npm run i18n && vite",
    "build": "npm run i18n && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.2.0"
  },
  "dependencies": {
    "bootstrap": "^5.3.3"
  }
}
